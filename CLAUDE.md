@AGENTS.md

# 팔로우미투어 — 프로젝트 규칙

기존 워드프레스 사이트(followmetour.co.kr)를 이관하는 게 아니라 Supabase + Vercel로
신규 구축 중. 배경은 `DESIGN.md`와 이 리포지토리 밖에 있는 마이그레이션 진단
아티팩트 참고.

## 스택
Next.js (App Router) + TypeScript + Tailwind v4 + Supabase (`@supabase/ssr`).

## 화면 구조 — 세 표면은 서로 다른 앱처럼 취급
- `src/app/(customer)/*` — 공개, 인증 없음. **모바일 전용** 타깃 (데스크톱은 안 깨지면 됨).
- `src/app/admin/*` — 스태프 인증. **데스크톱 + 모바일** 둘 다 실제로 씀.
- `src/app/guide/*` — 가이드 인증. **모바일 전용**, "오늘 내 일정" 하나로 압축된 화면.
  admin의 축소판으로 만들지 말 것 — 별개의 초경량 화면.

세 표면 다 `DESIGN.md`의 토큰(색상·폰트·spacing)을 공유하지만 밀도·톤은 다르게
적용한다 (`DESIGN.md` §5).

## 확정된 스코프 (다시 묻지 말 것)
- 회원가입 없음, 자사 홈페이지 예약 없음, 카카오·네이버 로그인 없음.
- 투어 카드 클릭 → 마이리얼트립 상품 URL로 연결 (관리자 화면에서 투어별로 URL 세팅).
- 후기: 완전 오픈폼 아님 — 투어명 + 투어일 최소 확인 후 작성, 모더레이션 큐 필요.
- 카카오 알림톡은 자체 구축 범위 밖 (필요 시 대행사 이용). 이메일 발송(예약 확정 메일)은 유지.
- 예약과 파트너예약은 하나의 테이블로 통합 (채널 컬럼으로 구분).
- 가이드 배정은 자유 텍스트가 아니라 가이드 계정(`guide` role)에 외래키로 연결.
- 기존 워드프레스 고객 PII(예약자 이름·이메일·연락처)는 이관하지 않고 파기.
- 비공개 투어는 삭제하지 않고 "고객 노출 여부" 플래그로만 `(customer)` 표면에서 숨김.
- 개발 순서: 관리자(`admin`) 먼저, 고객(`(customer)`)은 그다음, 가이드(`guide`)는 admin과
  같은 데이터 모델(일정·가이드 배정)에 의존하므로 admin 이후.

## 진행 순서 — 프론트엔드 먼저, 스키마는 그다음 (09-11 결정)
스키마를 먼저 확정하고 화면을 맞추는 대신, **화면을 먼저 만들어 피드백을 받고
그걸 토대로 스키마를 최종 확정**하는 순서로 진행 중. 그래서 지금 admin/guide/
customer 화면은 전부 `src/lib/sample-data.ts`의 목업 데이터를 쓰고, Supabase
Auth·쿼리는 아직 붙이지 않았다 — 화면 구조가 안정되기 전에 백엔드를 연결하면
피드백으로 갈아엎을 때 낭비가 크기 때문.

`supabase/migrations/20260911024723_init_schema.sql`은 **초안**이다. 실제
스키마(테이블/컬럼)는 화면 리뷰가 끝난 뒤 이 초안을 다시 보고 확정한다 —
지금 이 파일 내용을 최종으로 취급하지 말 것.

백엔드를 붙일 차례가 오면: `npm run db:start`(Docker 필요) → 초안 기준으로
스키마 확정 → `npm run db:types`로 `src/lib/supabase/database.types.ts` 생성
→ `src/lib/sample-data.ts`를 실제 쿼리로 교체.
