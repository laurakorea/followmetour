# DESIGN.md — 팔로우미투어 디자인 시스템

기존 `followmetour.co.kr`(워드프레스)의 로고와 브랜드 컬러를 기준점으로 삼되, 워드프레스 테마의 한계(나눔고딕 고정, 여백 없는 밀도, 장식 없는 사진 카드)는 버리고 요즘 웹 스탠다드로 다시 그린 시스템입니다. 신규 개발은 Supabase + Vercel, 화면은 세 개의 서로 다른 표면(고객 / 관리자 / 가이드)으로 나뉩니다 — 이 문서는 셋이 공유하는 토큰과, 표면별로 달라지는 적용 규칙을 함께 정의합니다.

## 0. 기존 브랜드에서 가져온 것

기존 사이트 로고(`wp-content/themes/fmt/images/brand.png`)를 실측한 값입니다.

- 워드마크 "followmetour" 소문자 표기, 색상 `#505060` 근처의 차분한 다크 그레이
- 로고 마크(하트/핀 결합 아이콘)의 포인트 컬러가 매우 채도 높은 레드 `#F01030` 근처
- 본문 폰트가 나눔고딕(NanumGothic) 고정, 링크·보조텍스트 색상 `#56565A`
- 카드 UI는 "사진 전면 + 하단 그라데이션 + 흰색 볼드 캡션" 패턴, 여백 없이 촘촘한 그리드

**유지할 것**: 핀/하트 결합 마크의 콘셉트, 레드 계열 포인트 컬러, 소문자 워드마크.
**버릴 것**: 나눔고딕, 무여백 촘촘한 레이아웃, 채도가 지나치게 높아 화면 전체에 쓰기엔 공격적인 레드 원색.

## 1. 컬러

포인트 레드는 유지하되 화면 전체(버튼, 배지, 강조 텍스트)에 반복해서 써도 피로하지 않도록 채도를 낮추고 톤을 살짝 로즈 쪽으로 옮겼습니다. 이 값이 세 화면 전부의 공통 기준입니다.

```css
:root {
  /* 브랜드 */
  --rose-600: #E31C4D;   /* 기존 로고 레드(#F01030)를 완화한 1차 브랜드 컬러 — 버튼, 링크, 로고 마크 */
  --rose-700: #B0123A;   /* hover/active, 로즈 위 텍스트 */
  --rose-100: #FCE8ED;   /* 배지·선택 상태 배경 */

  /* 뉴트럴 (따뜻한 웜그레이 — 순백 대신 아주 옅은 웜톤) */
  --paper: #FAF9F7;
  --surface: #FFFFFF;
  --ink-900: #1C1D24;    /* 본문 제목 */
  --ink-700: #454852;    /* 본문 텍스트 (기존 #56565A보다 살짝 진하게, 대비 개선) */
  --ink-500: #7A7D87;    /* 캡션·보조 정보 */
  --line: #E7E4E0;
  --line-strong: #D2CEC8;

  /* 의미색 (브랜드 로즈와 절대 겹치지 않게 — 관리자 대시보드 상태 표시 전용) */
  --good: #2F7D4F;
  --good-wash: #E4F0E7;
  --warn: #B9832A;
  --warn-wash: #F5EBD8;
  --critical: #C2410C;  /* 브랜드 로즈와 혼동되지 않도록 오렌지 계열로 분리 */
  --critical-wash: #FBE8DE;
}
```

다크 모드는 필수 스코프 아님. 관리자 화면에 야간 사용 요구가 생기면 위 토큰을 반전해 추가 — 지금은 라이트 테마 하나로 세 화면 모두 통일.

## 2. 타이포그래피

나눔고딕을 걷어내고, 세 화면이 같은 본문/UI 폰트를 공유하되 고객 화면만 헤드라인에 개성 있는 디스플레이 서체를 더합니다.

```css
--font-display: 'Gowun Dodum', 'Apple SD Gothic Neo', sans-serif; /* 고객 화면 헤드라인 전용 */
--font-body:    'IBM Plex Sans KR', 'Apple SD Gothic Neo', 'Malgun Gothic', sans-serif; /* 본문·UI 전체 공용 */
--font-mono:    'IBM Plex Mono', ui-monospace, monospace; /* 금액·날짜·정산 수치 (관리자) */
```

```html
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gowun+Dodum&family=IBM+Plex+Sans+KR:wght@400;500;600;700&family=IBM+Plex+Mono:wght@400;500;600&display=swap">
```

- **고객 화면**: 투어명·섹션 타이틀은 `--font-display`(둥글고 따뜻한 인상, 나눔고딕보다 확실히 "요즘 느낌"), 본문은 `--font-body`.
- **관리자·가이드 화면**: 전부 `--font-body`만 사용. 대시보드는 읽는 화면이 아니라 스캔하는 화면이라 서체를 하나로 통일하는 게 정보 인식 속도에 유리함.
- 표 안의 숫자(정산 금액, 인원, 날짜)는 항상 `--font-mono` + `font-variant-numeric: tabular-nums`로 자릿수를 맞춤.

타입 스케일(공용):

| 토큰 | 크기 | 용도 |
|---|---|---|
| `--text-2xl` | 28px | 고객 화면 히어로 타이틀 |
| `--text-xl` | 21px | 섹션 타이틀, 관리자 페이지 타이틀 |
| `--text-lg` | 17px | 카드 타이틀, 폼 라벨 |
| `--text-base` | 15px | 본문 |
| `--text-sm` | 13px | 캡션, 테이블 헤더 |
| `--text-xs` | 11.5px | 배지, 타임스탬프 |

## 3. 레이아웃 · 간격

8px 기준 스페이싱 스케일(`4/8/12/16/24/32/48/64`)을 세 화면 공용으로 사용. 반경은 `--radius-sm: 8px`(버튼·인풋), `--radius-md: 12px`(카드), `--radius-full: 999px`(배지·필).

- 카드/테이블/버튼의 그림자는 한 단계만: `--shadow: 0 1px 2px rgba(28,29,36,.06), 0 6px 20px -10px rgba(28,29,36,.18)`. 관리자 화면의 모든 패널에 그림자를 주지 말 것 — 강조가 필요한 요소(모달, 드롭다운)에만.

## 4. 표면별 반응형 전략 — 셋이 다르다

세 화면은 사용자도, 기기도, 목적도 다르므로 **반응형 대응 범위 자체를 다르게 정의**합니다.

| 표면 | 사용자 | 대응 범위 | 이유 |
|---|---|---|---|
| **관리자** | 스태프(사무실) | **데스크톱 + 모바일** 둘 다 | 사무실에서 데스크톱으로 정산·일정 관리, 외부에서 모바일로 확인하는 경우 둘 다 실제로 발생 |
| **가이드** | 현장 가이드 | **모바일 전용** (단일 브레이크포인트) | 현장에서 스마트폰으로만 씀. 데스크톱 레이아웃을 따로 설계·검증할 이유가 없음 |
| **고객** | 사이트 방문객 | **모바일 전용** (단일 브레이크포인트) | 투어 검색·후기 열람·마이리얼트립 연결이 목적 — 검색 유입 트래픽은 모바일이 압도적이라 데스크톱 대응은 우선순위 아님 |

```css
/* 관리자 전용 — 데스크톱 기준으로 설계하고 아래로 축소 */
--bp-admin-desktop: 1024px;   /* 사이드바 상시 노출, 테이블 전체 컬럼 */
--bp-admin-tablet:   768px;   /* 사이드바 접힘, 테이블 가로 스크롤 */
--bp-admin-mobile:   480px;   /* 카드형으로 재배치 */

/* 가이드 / 고객 — 모바일 단일 타깃, 폭 제약 없이 유동 */
--target-width: 360px ~ 430px;  /* 실제 기기 폭 기준으로만 검증, 넓은 화면에서 깨지지만 않으면 됨 */
```

**관리자**는 정보 밀도가 높은 화면(예약 표, 정산 표)이라 데스크톱이 1차 타깃이고, 모바일은 "확인용" 축소 뷰(테이블 → 카드 리스트 전환)로 설계합니다. **가이드**와 **고객**은 반대로 모바일이 유일한 타깃이라, 데스크톱 대응에 시간을 쓰지 않고 대신 모바일 안에서의 완성도(터치 영역 44px 이상, 엄지 도달 범위에 주요 액션 배치)에 집중합니다.

## 5. 표면별 톤 — 같은 시스템, 다른 표현

같은 컬러·타입 토큰을 쓰지만 화면마다 밀도와 태도가 다릅니다.

### 고객 (마케팅·정보 전달)
- 사진이 주인공. 기존 사이트의 "사진 카드 + 캡션" 문법은 유지하되, 여백을 넉넉히 주고 캡션 타이포를 `--font-display`로 바꿔 고급스럽게.
- 버튼은 `--rose-600` 단색, 큼직하게. 후기 작성·마이리얼트립 연결처럼 액션이 명확한 곳에만 사용.
- 투어 카드에 마이리얼트립 URL이 비어 있으면 카드 자체를 숨기거나 "준비중" 배지 처리 — 죽은 링크 노출 금지.

### 관리자 (운영 도구)
- 밀도 우선. 카드 남발 금지 — 목록은 테이블, 요약 수치만 스탯 타일.
- 상태는 색으로 말한다: `--good`/`--warn`/--critical`을 배지(필)로, 브랜드 로즈는 상태 표시에 쓰지 않음(액션 버튼·활성 탭 전용).
- 좌측 내비게이션은 기존 워드프레스의 평면 메뉴 나열 대신 업무 단위로 그룹핑: **예약 운영**(일정·파트너예약) / **정산**(수입관리·티켓관리) / **콘텐츠**(후기 모더레이션·투어) / **시스템**(계정).

### 가이드 (현장 모바일 도구)
- 화면은 사실상 "오늘 내 일정" 하나로 압축. 목록이 아니라 오늘 담당 투어 카드가 첫 화면.
- 큰 터치 타깃, 최소 텍스트. 관리자 화면의 축소판이 아니라 완전히 별개의 초경량 화면으로 설계.
- 로즈 컬러는 "완료 처리" 같은 유일한 주요 액션 버튼에만 — 나머지는 뉴트럴.

## 6. 로고

기존 핀/하트 결합 마크와 소문자 워드마크 콘셉트는 유지. 다만 원본 `#F01030`은 원색 그대로 벡터 재작업하고, 화면 안에서 반복 사용하는 포인트 컬러는 위 `--rose-600`(완화된 톤)을 쓰는 걸 권장 — 로고 자체는 원래의 선명한 레드를 유지해 브랜드 임팩트를 지키고, UI 컴포넌트(버튼·배지)는 완화된 톤을 써서 화면 전체가 피로해지지 않도록 분리합니다.
